<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spring.mta.dao.UserDao">

		<!-- 로그인  -->
		<select id="login" parameterType="user" resultType="user">
			select user_id, user_passwd, user_phone, user_email, user_name, to_char(user_regdate,'YYYY-MM-DD') as user_regdate, profile_image
			 from user_info
			 where user_id = #{user_id} and user_passwd = #{user_passwd} 
		</select>
		
		<update id="userUpdate" parameterType="user">
			update user_info
			set user_passwd = #{user_passwd}, profile_image = #{profile_image},
			 user_phone = #{user_phone},
			 user_email = #{user_email},
			user_regdate = sysdate 
			  where user_id = #{user_id}
		</update>
		
</mapper>